server:
  port: 8083

spring:
  application:
    name: plan
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/plan
    username: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
        show_sql: 'true'  # Corrected line
