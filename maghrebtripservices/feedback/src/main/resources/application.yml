server:
  port: ${microservices.feedback.port}

spring:
  profiles:
    active: dev
  application:
    name: feedback
  config:
    import: optional:configserver:http://localhost:8888
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    enabled: true
