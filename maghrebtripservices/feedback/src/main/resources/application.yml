server:
  port: ${microservices.feedback.port}

spring:
  profiles:
    active: default
  application:
    name: feedback
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:postgresql://localhost:${datasources.postgresql.port}/feedback
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:${microservices.eureka-server.port}/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true